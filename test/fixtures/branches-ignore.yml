name: CI
on:
  push:
    branches-ignore:
      - "dependabot/**"
    paths-ignore:
      - "docs/**"

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm test
